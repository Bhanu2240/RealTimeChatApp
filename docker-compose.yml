version: "3.8"

services:
  backend:
    build: ./Backend
    container_name: backend_container

    ports:
      - "5001:5001"

    restart: always

  frontend:
    build: ./Frontend
    container_name: frontend_container

    ports:
      - "5173:5173"

    restart: always

    depends_on:
      - backend